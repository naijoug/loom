# Loom Agent 指南

## 当前范围与文档入口

Loom 是 Tauri + React + Rust 的本地 Agent 工作台。当前交付优先可靠的单 Agent Chat；多 Agent 规划、实施 Review、调试修复属于独立的高级 Task 工作流。

- 产品范围以 [需求文档](docs/requirements.md) 为准；用户明确更新的要求优先于仓库约定。计划描述实施安排，历史计划不覆盖现行需求。
- 按任务需要查阅：[模块边界](docs/architecture.md)、[Chat 契约](docs/architecture/chat-contracts.md)、[Agent 适配](docs/agent-adapter.md)、[安全策略](docs/security-policy.md)。无需每次编辑都读取全部文档。
- [计划索引](docs/PLANS.md) 标明当前实施入口；记录现状、目标和历史证据时分别标注，不能用计划代替已完成证明。

## 执行与完成

- 按用户请求的范围交付：审查请求给出发现与建议；实施请求持续完成修改、相关验证和必要修复。
- 已授权范围内的可逆操作不重复确认；只有缺失信息会实质改变结果、操作超出授权或运行环境要求审批时才询问，并继续不受影响的工作。
- 用户补充信息默认修正当前任务；回答中途问题后继续原目标，除非用户明确停止或替换目标。
- 技能按实际任务触发，用户明确要求优先于技能中的工作流建议；因约束暂停时指出具体来源和阻塞动作。技能不能扩大权限或覆盖运行环境的安全边界。
- 子 Agent 仅在用户或适用指令允许时使用；委派任务应独立、有边界且能节省时间或改善质量。单 Agent Chat 不依赖委派。
- 默认用中文简洁报告结果、修改范围、验证证据和未解决限制；不把未运行的检查写成通过。

## 技术与数据边界

- 本地进程、文件系统、项目分析、日志和持久化放在 Rust/Tauri commands；交互、导航和可视化放在前端。
- Agent 集成通过适配器，保持多 CLI / 多模型兼容；不在 UI 拼接 CLI 参数，不以提示词替代权限校验。
- 保留用户已有改动。优先小而可逆的变更；新增依赖需有当前需求理由。
- Agent 调用和调试命令保留可见、脱敏的执行记录；任务历史、Review、反馈和验证证据需持久化。
- 设计稿与相关资源统一放在 `designs/`。

## 计划、预览与验证

- 非平凡功能、重构或迁移先写可独立执行和 Review 的中文计划；小修复可直接实施。计划路径、模板和索引规则见 [开发指南](docs/development.md)。
- 新建或更新计划时同轮更新 `docs/PLANS.md`，日期与同日时间均倒序。
- 预览统一使用 `scripts/debug.sh`（桌面）或 `scripts/debug.sh web`；启动前、验证结束后执行 `scripts/debug.sh stop`。固定端口 `1420`，辅助脚本和日志见 [脚本说明](scripts/README.md)。
- 完成前按 [验证矩阵](docs/testing.md#开发变更验证矩阵) 运行相关检查。通过后仅因新改动、失败或未解决风险扩大或重复验证；里程碑交付与发布保留完整门禁。
